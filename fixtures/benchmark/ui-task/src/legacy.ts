export function legacyPanel(title: string, body: string): string {
  return `<section class="legacy"><h2>${title}</h2><p>${body}</p></section>`;
}

export const legacySpacing = [4, 8, 12, 16, 24, 32];
